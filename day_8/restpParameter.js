const sum = (...args)=> {
    console.log(args);
    args.forEach(
        (ele)=>{
            sum +=ele;
        }
    )
}
const isDiv =(...args) => {
    console.log(args);
    let arr = {};
    args.forEach{
        (ele)
    }
}
SubmitEvent(1, 2, 3, 4, 5, 6, 7, 8, 89, 90, 12)